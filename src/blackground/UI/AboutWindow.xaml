<Window x:Class="blackground.UI.AboutWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="About blackground"
        Width="380" Height="380"
        ResizeMode="NoResize"
        WindowStartupLocation="CenterScreen"
        ShowInTaskbar="False">
    <Grid Margin="20">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <Image Grid.Row="0" Source="pack://application:,,,/Resources/logo.png"
               Width="120" Height="120" HorizontalAlignment="Center" Margin="0,0,0,12"/>

        <TextBlock Grid.Row="1" Text="blackground" FontSize="20" FontWeight="Bold"
                   HorizontalAlignment="Center" Margin="0,0,0,2"/>
        <TextBlock Grid.Row="2" Text="Version 1.0"
                   HorizontalAlignment="Center" Foreground="#666" Margin="0,0,0,12"/>
        <TextBlock Grid.Row="3" Text="A hotkey-driven blackout overlay."
                   HorizontalAlignment="Center" TextWrapping="Wrap" TextAlignment="Center"
                   Margin="0,0,0,12"/>

        <StackPanel Grid.Row="4" Orientation="Horizontal" HorizontalAlignment="Center">
            <TextBlock Text="Current Hotkey: " FontWeight="Bold"/>
            <TextBlock x:Name="HotkeyText"/>
        </StackPanel>

        <Button Grid.Row="5" Content="Close" Width="90" HorizontalAlignment="Right"
                Margin="0,12,0,0" IsCancel="True" IsDefault="True" Click="CloseButton_Click"/>
    </Grid>
</Window>
